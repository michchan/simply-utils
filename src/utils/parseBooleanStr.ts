/**
 * Parse a boolean string ('true'/'false') to boolean
 *
 * @param str The boolean string to parse
 * @category utils
 * @module parseBooleanStr
 * @category utils
 * @module parseBooleanStr
 */
const parseBooleanStr = (str: string): boolean => str === 'true'

export default parseBooleanStr